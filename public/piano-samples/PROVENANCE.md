# Pinned intake evidence

Accepted source checkout: `tambien/Piano` at commit
`0cd2c034f820c53e83ab22f5c13bd490b9e4de85`.

The intake command requires `PIANO_SOURCE_DIR` to point to a git checkout at
that exact HEAD and verifies its `README.md` and `LICENSE.md` before copying
MP3 files. The adaptation code is MIT; the derived Salamander Yamaha C5 audio
is CC BY 3.0 by Alexander Holm. Runtime validation is offline and checks the
committed bytes, hashes, roles, paths, and package output.
